import React from 'react'
import { Title, SubTitle, Separator } from '../components/kate-style-components/'

export default class Unify extends React.Component {
    render() {
        return (
            <div>
                <Title text="Unify" />
                <Separator />
                <SubTitle text="Unify is unifying sth sth sth darkside" />
            </div>
        )
    }
}